export default class Todo {
  constructor(title, creations, type, completed, id) {
    this.title = title;
    this.creations = creations;
    this.type = type;
    this.completed = completed;
    this.id = id;
  }
}
